import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  card: {
    width: 300,
    marginBottom: 20,
    zIndex: 0,
  },
  media: {
    height: 350,
    zIndex: 0,
  },
  btnLink: {
    color: "white",
    zIndex: 0,
  },
});
